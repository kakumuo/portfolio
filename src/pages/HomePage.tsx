import { MainPage } from "../components/MainPage";

export function HomePage(){
    return <MainPage>
        Something
    </MainPage>
}

const styles = {
    container: `h-full w-full grid`,
}